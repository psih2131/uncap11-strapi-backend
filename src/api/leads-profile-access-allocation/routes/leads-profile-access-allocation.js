'use strict';

/**
 * leads-profile-access-allocation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::leads-profile-access-allocation.leads-profile-access-allocation');
