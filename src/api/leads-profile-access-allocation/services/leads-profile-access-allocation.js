'use strict';

/**
 * leads-profile-access-allocation service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::leads-profile-access-allocation.leads-profile-access-allocation');
